
ds.boost_step.R <- function(){
  # choose split points
  
  # calculate histograms locally
  
  # calculate split score at client
  
  # save tree extension
  
  # if not pos split score -> save tree
}